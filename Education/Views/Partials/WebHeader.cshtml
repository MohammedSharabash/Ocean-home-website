
<nav class="navbar navbar-expand-lg bg-white navbar-light shadow sticky-top p-0">
    <a href="/Home/Index" class="navbar-brand d-flex align-items-center px-4 px-lg-5">
        <h2 class="m-0 text-primary"><i class="fa fa-book me-3"></i>Ocean Home</h2>
    </a>
    <button type="button" class="navbar-toggler me-4" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav ms-auto p-4 p-lg-0">
            <a href="/Home/Index" class="nav-item nav-link  @(Context.Request.Path.ToString().ToLower().Contains("index") ? "active" : "")">Home</a>
            <a href="/Home/About" class="nav-item nav-link @(Context.Request.Path.ToString().ToLower().Contains("about") ? "active" : "")">About</a>
            <a href="/Home/Courses" class="nav-item nav-link @(Context.Request.Path.ToString().ToLower().Contains("courses") ? "active" : "")">Courses</a>
            <div class="nav-item dropdown"></div>
            <a href="/Home/Contact" class="nav-item nav-link @(Context.Request.Path.ToString().ToLower().Contains("contact") ? "active" : "")">Contact</a>
            <a href="/Home/IndexAr" class="nav-item nav-link ponter @(Context.Request.Path.ToString().ToLower().Contains("indexar") ? "active" : "")">Arabic</a>
            @*<div class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Pages</a>
            <div class="dropdown-menu fade-down m-0">
            <a href="team.html" class="dropdown-item">Our Team</a>
            <a href="testimonial.html" class="dropdown-item">Testimonial</a>
            <a href="404.html" class="dropdown-item">404 Page</a>
            </div>
            </div>*@

            @if (User.Identity.IsAuthenticated)
            {
                <div class="profile-container d-flex justify-content-center align-items-center gap-2 " onclick="showSett()">
                    <img src="~/assets/img/avatardefault_92824.png" />
                    <h3>My Profile</h3>
                    <i class="arr do"></i>
                    <div class="sett d-flex flex-column  gap-1 align-items-center justify-content-center" id="set">
                        <a href="/Home/Profile"><h3>setting</h3></a>
                        <a href="/Home/LogOut"><h3>logout</h3></a>
                        <h3></h3>
                    </div>
                </div>
            }
            else
            {
                <a href="/Home/Register" class="btn btn-primary py-4 px-lg-5  d-lg-block">Join Now<i class="fa fa-arrow-right ms-3"></i></a>
            }
        </div>
        @* @if (User.Identity.IsAuthenticated)
        {
        <a href="/Home/LogOut" title="LogOut" class="btn  py-4 px-lg-5 d-none d-lg-block "><i class="fa fa-arrow-left  ms-3"></i>log out</a>

        <a href="/Home/Profile" class="btn btn-primary py-4 px-lg-5 d-none d-lg-block">My Profile<i class="fa fa-arrow-right ms-3"></i></a>
        }
        else
        {
        <a href="/Home/Register" class="btn btn-primary py-4 px-lg-5 d-none d-lg-block">Join Now<i class="fa fa-arrow-right ms-3"></i></a>

        } *@
    </div>
</nav>
